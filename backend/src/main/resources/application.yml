server:
  port: 8080

spring:
  application:
    name: account-dashboard-backend

# CORS origins allowed to call the API in development (Vite dev server).
app:
  cors:
    allowed-origins: http://localhost:5173

management:
  endpoints:
    web:
      exposure:
        include: health,info
